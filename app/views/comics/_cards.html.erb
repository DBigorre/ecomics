<div class="card-trip">
  <%= image_tag comic.illustration %>
  <div class="card-trip-infos">
    <div>
      <h2><%= comic.name %></h2>
    </div>
    <h2 class="card-trip-pricing"><%= number_to_currency(comic.price, unit: '€') %></h2>
  </div>
</div>
